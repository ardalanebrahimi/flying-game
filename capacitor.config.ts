import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ardiland.flyinggame',
  appName: 'flying-game',
  webDir: 'dist/flying-game/browser',
  bundledWebRuntime: false,
};

export default config;
